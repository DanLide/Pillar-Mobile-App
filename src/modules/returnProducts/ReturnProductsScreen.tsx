import React from 'react';
import { StyleSheet, View } from 'react-native';

export const ReturnProductsScreen: React.FC = () => (
  <View style={styles.container} />
);

const styles = StyleSheet.create({
  container: { flex: 1 },
});
