export const idContainer = (testID: string) => `${testID}:container`;

export const idTitle = (testID: string) => `${testID}:title`;

export const idContent = (testID: string) => `${testID}:content`;

export const idLoadingIndicator = (testID: string) =>
  `${testID}:loadingIndicator`;

export const idClose = (testID: string) => `${testID}:close`;

export const idButton = (testID: string) => `${testID}:button`;

export const idCount = (testID: string) => `${testID}:count`;

export const idCloseIcon = (testID: string) => `${testID}:closeIcon`;

export const idUndoText = (testID: string) => `${testID}:undoText`;

export const idSettingsText = (testID: string) => `${testID}:settingsText`;

export const idRetryText = (testID: string) => `${testID}:retryText`;

export const idPrimaryButton = (testID: string) => `${testID}:primaryButton`;

export const idSecondaryButton = (testID: string) =>
  `${testID}:secondaryButton`;

export const idModal = (testID: string) => `${testID}:modal`;

export const idDetailsButton = (testID: string) => `${testID}:idDetailsButton`;
